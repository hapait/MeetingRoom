import { StyleSheet, Dimensions } from 'react';

//const { height, width } = Dimensions.get('window');

//export const ResizableStyle = StyleSheet.create({
export const ResizableStyle = {
    resizablediv: {
        width: 100,
        height: 100,
        borderColor: '#333333'
    }
}